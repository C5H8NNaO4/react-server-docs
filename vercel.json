{
  "rewrites": [{ "source": "/(.*)", "destination": "/api/index.js" }],
  "builds": [
    {
      "src": "./api/index.js",
      "use": "@vercel/node"
    },
    {
      "src": "/vercel/output/dist/server/**/*.js",
      "use": "@vercel/node",
      "config": { "output": { "root": "./dist/server" } }
    }
  ],
  "outputDirectory": ".",
  "devCommand": "node api/index",
  "buildCommand": "yarn build:server && yarn build:client"
}
